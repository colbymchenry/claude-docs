{
  "name": "claude-docs",
  "version": "1.0.0",
  "description": "A global MCP server that gives Claude a persistent, per-project knowledge base",
  "type": "module",
  "bin": {
    "claude-docs": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "postinstall": "node scripts/postinstall.mjs",
    "prepublishOnly": "npm run build"
  },
  "files": [
    "dist",
    "scripts"
  ],
  "keywords": [
    "claude",
    "mcp",
    "documentation",
    "knowledge-base"
  ],
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "@vscode/ripgrep": "^1.15.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.5.0"
  }
}
